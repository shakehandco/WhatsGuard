{
  "name": "whatsguard",
  "version": "0.3.1",
  "description": "Privacy-preserving, on-device elder-fraud detection for WhatsApp.",
  "author": "WhatsGuard",
  "license": "Apache-2.0",
  "private": true,
  "main": "./out/main/index.js",
  "type": "commonjs",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "typecheck:node": "tsc --noEmit -p tsconfig.node.json",
    "typecheck": "npm run typecheck:node",
    "start": "electron-vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "pack:mac": "npm run build && electron-builder --mac --publish never",
    "pack:mac:release": "npm run build && electron-builder --mac -c.mac.notarize=true --publish never"
  },
  "dependencies": {
    "baileys": "^7.0.0-rc14",
    "electron-updater": "^6.8.9",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "@types/qrcode": "^1.5.6",
    "electron": "^43.0.0",
    "electron-builder": "^26.15.3",
    "electron-vite": "^5.0.0",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vitest": "^4.1.9"
  },
  "overrides": {
    "tar": "^7.5.11",
    "form-data": "^4.0.6",
    "protobufjs": "^7.6.4"
  }
}
